import { Container } from 'react-bootstrap';

const Header = (): JSX.Element => {
  return (
    <Container as="header" className={`shrink-max-width-xxl`}>
      Header
    </Container>
  );
};

export default Header;
